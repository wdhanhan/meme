# 推荐：使用 meme-c/.sms.secrets（5 行纯值，见 .sms.secrets.example），并设 systemd Environment=MEMEC_SMS_SECRETS_FILE=...（deploy.sh 已写）。
# 若需用 KEY= 形式覆盖，可使用 meme-c/.env.sms.local（与 deploy 加载顺序以 deploy.sh 为准）。

ALIYUN_SMS_ACCESS_KEY_ID=
ALIYUN_SMS_ACCESS_KEY_SECRET=
ALIYUN_SMS_SIGN_NAME=南京千雄科技
ALIYUN_SMS_TEMPLATE_CODE=SMS_332240891
# ALIYUN_SMS_REGION_ID=cn-hangzhou
